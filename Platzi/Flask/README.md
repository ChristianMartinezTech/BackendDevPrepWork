# Platzi's Flask Course

### Installation
* sudo apt install python3.10-venv
* python3 -m venv flask-env
* source flask-env/bin/activate
* pip install Flask
* python3 -m flask --version
* pip freeze > requirements.txt
* export FLASK_APP=main.py

### Venv activation
* source flask-env/bin/activate
